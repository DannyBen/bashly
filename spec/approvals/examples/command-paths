+ bashly generate
creating user files in src
created src/commands/download.sh
created src/commands/upload.sh
created ./cli
run ./cli --help to test your bash script
+ ./cli
cli - Sample application

Usage:
  cli COMMAND
  cli [COMMAND] --help | -h
  cli --version | -v

Commands:
  download   Download a file
  upload     Upload a file

+ ./cli -h
cli - Sample application

Usage:
  cli COMMAND
  cli [COMMAND] --help | -h
  cli --version | -v

Commands:
  download   Download a file
  upload     Upload a file

Options:
  --help, -h
    Show this help

  --version, -v
    Show version number

Environment Variables:
  API_KEY
    Set your API key

+ ./cli download something
# this file is located in 'src/commands/download.sh'
# code for 'cli download' goes here
# you can edit it freely and regenerate (it will not be overwritten)
args:
- ${args[source]} = something
+ ls -1 src/bashly.yml src/commands
src/bashly.yml

src/commands:
download.sh
upload.sh
