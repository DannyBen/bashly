<%= view_marker %>
case "${1:-}" in
% if root_command?
% if short_flag_exist? "-v"
--version )
% else
--version | -v )
% end
  version_command
  exit
  ;;

% end
% if short_flag_exist? "-h"
--help )
% else
--help | -h )
% end
  long_usage=yes
  <%= function_name %>_usage
  exit
  ;;

esac