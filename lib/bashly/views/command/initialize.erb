# :command.initialize
initialize() {
  version="<%= version %>"
  long_usage=''
  <%= ENV['BASHLY_STRICT'] ? "set -euo pipefail" : "set -e" %>

<%= load_user_file("initialize.sh", placeholder: false).indent 2 %>
}
