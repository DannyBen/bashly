# :flag.validations
% if validate
if [[ -n $(validate_<%= validate %> "$2") ]]; then
  printf "<%= strings[:validation_error] %>\n" "<%= usage_string %>" "$(validate_<%= validate %> "$2")"
  exit 1
fi

% end