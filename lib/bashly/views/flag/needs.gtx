= view_marker

> if [[ -n ${args['{{ name }}']+x} ]]; then
>   for need in {{ needs.join ' ' }}; do
>     if [[ -z "${args[$need]:-}" ]]; then
>       printf "%s\n" "{{ strings[:flag_requires_another] % { name: name, need: "$need" } }}" >&2
>       exit 1
>     fi
>   done
> fi
>